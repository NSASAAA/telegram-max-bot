services:
  bot:
    build: .
    env_file:
      - .env
    environment:
      PYTHONPATH: /app/src
    volumes:
      - ./data:/app/data
    command: python -m telegram_max_bot.main
    restart: unless-stopped
    mem_limit: 256m
    cpus: "0.50"

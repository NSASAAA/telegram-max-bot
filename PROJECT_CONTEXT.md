# Project Context

## Проект

`telegram-max-bot` — Python-проект бота для Telegram с перспективой добавления MAX.

Текущий практический фокус: Telegram-бот, который работает с RSS-лентой статей, сохраняет материалы в SQLite и показывает их пользователю через команды.

Долгосрочная цель: сделать не просто репостер статей, а коммуникационную систему вокруг автора и его аудитории.

## Продуктовая идея

Изначальная идея: автоматически перепубликовывать статьи из Яндекс Дзена в Telegram, а позже в MAX.

После обсуждения концепция расширена: бот должен стать точкой контакта между автором и читателями. Статьи являются поводом для общения, а не конечной целью.

Будущий бот должен помогать:

- показывать новые статьи;
- находить материалы автора;
- собирать вопросы читателей;
- принимать отклики на статьи;
- собирать предложения тем;
- принимать истории читателей;
- показывать ответы автора;
- поддерживать проект.

Тематика автора: вера, семья, воспитание, материнство, боль, личные границы, психологическая зрелость, социальные роли, феминизм, свобода от разрушительных установок.

Бот не должен превращаться в простой "православный календарь" или пассивный архив. Более точное позиционирование: проводник по текстам автора и канал живой обратной связи.

## Корпус Дзена заказчика

28 мая 2026 собран локальный сырой архив канала Дзена:

```text
https://dzen.ru/1234elena
```

Канал: `С матушкой за чаем`.

По описанию канала автор позиционирует себя как супруга православного священника, которая пишет о жизни обычных людей, рассеянном склерозе, инвалидности и близких.

Локальные файлы архива:

```text
data/raw/dzen_1234elena/article_links.json
data/raw/dzen_1234elena/articles_sample_20.jsonl
data/raw/dzen_1234elena/articles_full.jsonl
```

`data/` не должен попадать в Git. Архив нужен локально для анализа, нормализации и последующего импорта в базу.

Состояние сырого архива:

- собрано `440` ссылок на статьи;
- собрано `440` материалов;
- ошибок загрузки: `0`;
- размер полного JSONL-файла: около `12 MB`;
- общий объем очищенного текста: около `1 006 842` символов;
- средняя длина статьи: около `2 288` символов;
- медианная длина статьи: около `1 878` символов.

Текущая структура каждой записи в `articles_full.jsonl`:

```text
source
channel_url
channel_name
url
title
published_label
views_label
reading_time_label
content_text_guess
raw_main_text
raw_body_text
images
scraped_at
```

`content_text_guess` - очищенный текст статьи. `raw_main_text` и `raw_body_text` содержат больше технического и интерфейсного шума Дзена.

Ограничения текущего архива:

- `published_label` пока хранится как человекочитаемая строка Дзена, например `Сегодня`, `Вчера`, `7 февраля`;
- `views_label` нужно нормализовать в число;
- комментарии можно извлекать только приблизительно из текста страницы;
- лайки надежно не извлечены;
- часть очень коротких материалов нужно проверить вручную: это могут быть как реальные короткие заметки, так и ограничения парсинга;
- перед импортом в боевую базу нужен отдельный этап нормализации.

## Локальная база Дзена и изображения

28 мая 2026 выполнена локальная подготовка реального корпуса Дзена.

Созданы локальные артефакты:

```text
data/dzen_bot.db
data/media/dzen_1234elena/
data/media/dzen_1234elena/images_manifest.jsonl
```

Результат локального импорта:

- статей в локальной базе: `440`;
- записей в `article_images`: `976`;
- скачанных изображений: `976`;
- ошибок скачивания изображений: `0`;
- статей с выбранной обложкой `cover`: `426`;
- статей без подходящей обложки: `14`;
- размер локальной базы `data/dzen_bot.db`: около `3.2 MB`;
- размер media-папки: около `248 MB`.

Важное решение: картинки не хранятся бинарно внутри SQLite. В базе хранятся пути к файлам и метаданные, а сами изображения лежат на диске в `data/media/`.

Схема хранения:

```text
posts
article_images
data/media/dzen_1234elena/articles/<article_id>/001-cover.jpg
data/media/dzen_1234elena/articles/<article_id>/002-inline.jpg
```

Для каждой картинки сохраняются:

```text
article_link
article_source_id
source_url
local_path
position
role
alt_text
mime_type
file_size
sha256
download_status
error
```

Фильтрация изображений:

- используются реальные изображения статей с `avatars.dzeninfra.ru/get-zen_doc/...`;
- служебные иконки, emoji, favicon, рекламные картинки и `data:image/...` не сохраняются;
- если у одной картинки есть варианты `smart_crop` и `scale_1200`, предпочтение отдается более крупной версии `scale_1200`;
- первая картинка статьи получает роль `cover`, остальные - `inline`.

Для локальной проверки создан импорт:

```bash
PYTHONPATH=src python -m telegram_max_bot.dzen_import
```

По умолчанию он читает:

```text
data/raw/dzen_1234elena/articles_full.jsonl
```

и создает:

```text
data/dzen_bot.db
data/media/dzen_1234elena/
```

Перенос на VPS выполнен 28 мая 2026:

- текущая тестовая база Хабра сохранена в `data/backups/`;
- `data/dzen_bot.db` перенесена на VPS как `data/bot.db`;
- `data/media/dzen_1234elena/` перенесена на VPS;
- серверный `.env` обновлен: `SOURCE_RSS_URL=` и `CHECK_INTERVAL_SECONDS=0`, чтобы фоновый RSS-импорт Хабра не записывал тестовые материалы в новую базу Дзена;
- контейнер Docker перезапущен;
- внутри контейнера проверено: `440` статей, `976` изображений, `426` cover, `0` отсутствующих файлов изображений.

## Аналитика автора и блога

Первичный анализ корпуса показал, что проект не стоит понимать как обычный православный блог, календарь, сборник благочестивых заметок или технический репостинг Дзена.

Более точное смысловое ядро:

```text
Честный женский разговор о вере, семье, болезни, браке, материнстве,
личных границах и праве не разрушать себя ради чужих ожиданий.
```

Авторская позиция строится на важном напряжении:

- автор находится внутри православной среды и говорит из нее;
- одновременно автор не поддерживает глянцевую картинку "правильной матушки", "идеальной жены", "терпеливой женщины" или "святой семьи";
- вера в текстах не выглядит декорацией, но и не используется как инструмент давления;
- автор часто сопротивляется подмене смирения самоуничтожением;
- личная боль, болезнь мужа, семейная нагрузка и опыт материнства становятся не темой жалобы, а способом говорить с читателем честно.

Авторский голос:

- личный;
- прямой;
- разговорный;
- полемичный;
- эмоционально честный;
- иногда резкий;
- не наставнический в классическом смысле;
- часто построенный как диалог с читателем;
- ориентированный на снятие фальши и "правильной картинки".

Психологически авторский контракт с читателем можно описать так:

```text
Здесь можно говорить о том, о чем неудобно говорить в красивой,
правильной и благочестивой картинке жизни.
```

Для проекта это принципиально важно: бот не должен стать сухим каталогом статей. Он должен поддерживать живую интонацию автора и помогать читателю найти текст, вопрос, отклик или разговор, который соответствует его реальной жизненной ситуации.

## Основные тематические линии

### Рассеянный склероз, болезнь мужа, инвалидность

Это одна из самых сильных и узнаваемых линий блога. Читателям важны не только факты о болезни, но и опыт жизни рядом с хроническим диагнозом: быт, страх, усталость, любовь, ограничения, принятие, семейные решения.

Эта линия может стать отдельной навигационной рубрикой в боте:

```text
Жизнь рядом с РС
```

### Брак и семья под давлением реальности

В блоге много текстов о браке не как об идеальной форме, а как о живом союзе, где есть болезнь, усталость, дети, деньги, быт, сложные решения и невозможность соответствовать чужим ожиданиям.

Важный фокус: не "как построить идеальную семью", а "как жить в реальной семье, где все сложнее, чем принято показывать".

### Женская роль, самопожертвование, феминизм

Очень важная и перспективная линия. Автор говорит с женщинами, которые устали от требований быть удобными, терпеливыми, спасательницами, хранительницами всего и при этом не иметь права на усталость, злость или собственную жизнь.

Возможная рубрика:

```text
Женщина не обязана
```

### Психология, границы, травма, эмоциональная зрелость

В корпусе заметно много текстов, где личный опыт соединяется с психологическим языком: границы, нарциссизм, травма, созависимость, алекситимия, эмоциональная зрелость, вина, стыд, принятие.

Эта линия хорошо подходит для будущей навигации в боте, потому что читатель часто приходит не за датой публикации, а за ответом на внутреннее состояние.

### Дети, подростки, воспитание, образование

Большая часть корпуса так или иначе связана с детьми, подростками, семейным укладом, школой, домашним обучением, сыновьями и самостоятельностью.

Перспективная подача: не абстрактные советы по воспитанию, а честный родительский опыт без идеализации.

### Православие без насилия над собой

Православие в блоге важно, но сильнее всего оно работает не как отдельная справочная тема, а в связке с реальной жизнью: семья, болезнь, вина, смирение, женская роль, церковные стереотипы.

Возможная рубрика:

```text
Вера без насилия над собой
```

### Москва, экскурсии, культура, мягкий лайфстайл

Это более спокойная и потенциально монетизируемая ветка. Она может быть полезна для расширения аудитории, мягкого вовлечения и возможных будущих продуктов автора: прогулки, экскурсии, подборки мест, культурные маршруты, личный взгляд на город.

## Метрики и интерес аудитории

По всем `440` материалам удалось извлечь просмотры:

- всего просмотров в архиве: около `131 899`;
- среднее число просмотров на материал: около `300`;
- медиана просмотров: около `118`;
- максимум: около `39 800`.

Медиана важнее среднего, потому что несколько сильных материалов резко поднимают среднее значение.

Самые просматриваемые материалы корпуса:

```text
39 800 - "Ну, ты как?" - самый больной вопрос в моей жизни
8 926  - В любой момент вы с мужем должны быть готовы сделать ЭТО
2 530  - Давайте знакомиться
2 305  - У нас с мужем скорее всего больше не будет детей. Рассказываю почему
1 991  - Хочу рассказать про рассеянный склероз
1 608  - Вот кто оказывается виноват в низкой рождаемости
1 247  - Чему я научилась с болезнью мужа
1 227  - Про детей. Про семью. А может даже про это поговорим
1 178  - Мой быт с человеком с РС. Часть первая
1 110  - Привет. Давайте знакомиться
1 098  - Как мой муж справляется с рассеянным склерозом? Просили рассказать. Рассказываю
```

Предварительные выводы по метрикам:

- лучше всего срабатывают не абстрактные православные темы, а личные, болезненные и конфликтные тексты;
- сильные заголовки часто содержат напряжение, вопрос, боль, семейную дилемму или обещание личного признания;
- тема РС и жизни рядом с болезнью является одним из главных драйверов внимания;
- темы брака, женской роли, самопожертвования и психологических границ выглядят сильными для удержания аудитории;
- интерактивные тексты и вопросы к аудитории могут собирать много комментариев даже при умеренных просмотрах;
- комментарии и просмотры измеряют разные типы интереса: просмотры показывают охват, комментарии показывают готовность вступить в отношения с автором.

Пример важного сигнала: статья `А как вы познакомились с мужем?` имела умеренные просмотры, но собрала много комментариев. Это означает, что простые личные вопросы могут быть сильным инструментом вовлечения.

## Гипотеза об аудитории

Вероятная центральная аудитория:

- женщины примерно `30-55`;
- замужние женщины или женщины с опытом сложных отношений;
- матери подростков или взрослых детей;
- читательницы из православной или околоцерковной среды;
- люди, уставшие от идеализированных образов семьи, веры и женственности;
- люди рядом с хронической болезнью, инвалидностью или тяжелым уходом;
- читатели, которым важен честный язык о вине, усталости, любви, границах и принятии.

Главные потребности аудитории:

- узнать себя в чужом опыте;
- получить разрешение не быть идеальной;
- найти язык для своей усталости, боли или злости;
- увидеть православную идентичность без давления и морализаторства;
- понять, где заканчивается смирение и начинается разрушение себя;
- почувствовать, что автор не сверху, а рядом;
- задать вопрос, рассказать свою историю, получить отклик.

Для удержания аудитории важно не только публиковать статьи, но и регулярно создавать ощущение разговора:

- вопросы к читателям;
- сбор историй;
- ответы автора;
- разборы писем;
- продолжения по мотивам комментариев;
- голосования по будущим темам;
- подборки старых текстов под актуальные жизненные ситуации.

## Продуктовое позиционирование после анализа

Бот должен развиваться как коммуникационная система вокруг автора, а не как техническая витрина Дзена.

Рабочее позиционирование:

```text
Бот "С матушкой за чаем" помогает читателю найти честные тексты
о вере, семье, болезни, браке, детях, женской роли и личных границах,
а также задать вопрос автору или откликнуться своей историей.
```

Важный принцип: статьи - это не финальная точка. Статьи являются поводом для общения.

Будущая петля вовлечения:

```text
Автор публикует текст
→ бот доставляет или рекомендует текст
→ читатель реагирует, задает вопрос или рассказывает историю
→ автор выбирает важные отклики
→ появляется новый текст, ответ или подборка
→ читатель видит, что его услышали
→ лояльность растет
```

## Контентная стратегия

Предварительный баланс будущего контента:

- `60%` - личные проблемные эссе: болезнь, брак, дети, вера, женская роль;
- `20%` - объясняющие тексты: РС, уход, семейные решения, психологические понятия;
- `10%` - интерактивные посты: вопросы, голосования, просьбы поделиться опытом;
- `10%` - мягкий лайфстайл: Москва, культура, прогулки, книги, кино, быт.

Сильные типы заголовков:

- личное признание;
- болезненный вопрос;
- семейная дилемма;
- конфликт между ожиданием и реальностью;
- конкретный опыт рядом с РС;
- разворот против привычного стереотипа;
- вопрос к читателю.

Слабее как самостоятельный двигатель роста могут работать слишком общие темы без личного напряжения: поздравления, нейтральные заметки, отвлеченные бытовые записи. Их можно оставлять для поддержания тепла, но не делать на них основную ставку продвижения.

## Как аналитика влияет на бота

Бот должен уметь работать не только с последними статьями, но и с тематическим смыслом корпуса.

Будущие разделы и кнопки:

```text
Последние статьи
Самые читаемые
Самые обсуждаемые
Случайная статья
Про РС и болезнь
Про брак и семью
Про детей
Про веру без насилия
Про женскую роль и границы
Про Москву и прогулки
Задать вопрос автору
Предложить тему
Поделиться историей
Поддержать проект
```

Для этого нужно добавить в базу и логику проекта:

- нормализованные просмотры;
- нормализованное время чтения;
- примерное число комментариев, если его удается извлечь;
- теги/рубрики;
- источник материала;
- дату публикации в нормальном формате;
- флаг качества извлечения текста;
- возможность отличать полные статьи от коротких или проблемно извлеченных материалов.

На текущем этапе уже добавлены проверочные команды:

```text
/top
/random
```

Рубрики и тематическая навигация пока не реализованы.

## Аналитические и этические ограничения

Темы проекта чувствительные: болезнь, инвалидность, вера, брак, психологические травмы, семейные конфликты, женская роль.

Поэтому в будущей логике бота важно:

- не выдавать медицинские советы от имени бота;
- не ставить психологические диагнозы читателям;
- не заменять консультацию специалиста;
- аккуратно обрабатывать личные истории;
- не публиковать пользовательские вопросы без согласия;
- дать автору возможность модерировать входящие отклики;
- сохранять тон проекта: честно, прямо, но без эксплуатации чужой боли.

## Архитектурный принцип

Бизнес-логика не должна зависеть напрямую от Telegram или MAX.

Правильная схема:

```text
Telegram API ─┐
              ├── adapters → core → storage/services
MAX API      ─┘
```

`core/` содержит общую бизнес-логику.

`adapters/telegram.py` только принимает Telegram-события, преобразует их во внутренние модели и отправляет ответы.

`adapters/max.py` пока остается заглушкой. MAX будет добавлен позже как второй транспорт.

## Текущая структура

```text
telegram-max-bot/
├── README.md
├── PROJECT_CONTEXT.md
├── .env.example
├── .gitignore
├── requirements.txt
├── Dockerfile
├── docker-compose.yml
├── deploy.sh
├── .github/
│   └── workflows/
│       └── deploy.yml
├── data/
│   └── bot.db
├── src/
│   └── telegram_max_bot/
│       ├── __init__.py
│       ├── main.py
│       ├── config.py
│       ├── db.py
│       ├── rss_client.py
│       ├── rss_import.py
│       ├── core/
│       │   ├── __init__.py
│       │   ├── bot.py
│       │   └── models.py
│       └── adapters/
│           ├── __init__.py
│           ├── telegram.py
│           └── max.py
└── tests/
    └── __init__.py
```

## Текущее состояние приложения

Сейчас работает Telegram-бот через `python-telegram-bot`.

Команды:

- `/start` — приветствие и основные команды;
- `/help` — список команд;
- `/about` — описание тестовой версии бота;
- `/articles` — последние статьи из SQLite-базы;
- `/check` — ручной запуск импорта RSS со статистикой;
- обычный текст — простой ответ о полученном сообщении.

`main.py` только загружает конфиг и запускает `TelegramAdapter`.

`config.py` загружает `.env` через `python-dotenv` и требует `TELEGRAM_BOT_TOKEN`.

`core/bot.py` содержит бизнес-логику команд.

`adapters/telegram.py` содержит Telegram-интеграцию и маршрутизацию команд.

`adapters/max.py` пока заглушка.

## RSS и база данных

Реализован контур:

```text
RSS → rss_import.py → SQLite → /articles
```

Тестовая RSS-лента сейчас:

```text
https://habr.com/ru/rss/articles/
```

Модули:

- `rss_client.py` читает RSS через `feedparser`;
- `rss_import.py` импортирует статьи из RSS;
- `db.py` работает с SQLite-базой `data/bot.db`.

На VPS папка `data/` подключена в контейнер как volume:

```text
/opt/bots/telegram-max-bot/data → /app/data
```

После ручного запуска импорта файл базы `bot.db` создается на VPS в `data/bot.db` и виден контейнеру как `/app/data/bot.db`.

Импорт поддерживается в двух режимах:

- ручной запуск:

```bash
cd /opt/bots/telegram-max-bot
docker compose exec -T bot python -m telegram_max_bot.rss_import
```

- фоновый автоимпорт в процессе бота:
  - если задан `SOURCE_RSS_URL`;
  - и `CHECK_INTERVAL_SECONDS > 0`;
  - цикл импорта запускается внутри `TelegramAdapter` через `post_init`.

Текущая модель статьи в базе:

```text
id
title
link
guid
published
updated
summary
summary_html
content_html
content_text
image_url
author
categories
source
source_guid
updated_at_from_feed
content_hash
created_at
first_seen_at
last_seen_at
last_changed_at
```

Текущая синхронизация при импорте:

- новая статья → создается запись (`created`);
- существующая статья с измененным `content_hash` → обновляется (`updated`);
- существующая без изменений → обновляется только `last_seen_at` (`unchanged`).

На текущем этапе SQLite — осознанный простой выбор: база хранится как файл `data/bot.db` на VPS и подключается в контейнер через volume.

Будущая задача на более зрелом этапе проекта: рассмотреть миграцию с SQLite на PostgreSQL, если появятся несколько воркеров/контейнеров, много параллельных записей, админка, сложная аналитика или потребность в более надежной серверной базе данных.

## Важные заметки по RSS

Импорт уже показывает статистику:

```text
Posts received: 40
New posts saved: 2
Posts updated: 1
Posts unchanged: 37
```

Проверено на VPS 28 мая 2026:

- команда `/check` успешно запускает импорт и возвращает статистику;
- фоновый автоимпорт работает по `CHECK_INTERVAL_SECONDS`;
- рабочий интервал в production: `CHECK_INTERVAL_SECONDS=300` (раз в 5 минут).

## Что пока не реализовано

Пока нет:

- автопубликации новых статей в Telegram-канал;
- гарантированного полного текста статьи для всех источников RSS;
- нормализованного импорта сырого архива Дзена в SQLite;
- тематических тегов и рубрик для статей;
- навигации по самым читаемым и самым обсуждаемым материалам;
- поиска по статьям;
- случайной статьи;
- пользовательского меню с кнопками;
- сбора вопросов, откликов и историй читателей;
- MAX-интеграции.

## Инфраструктура

Код хранится в GitHub:

```text
NSASAAA/telegram-max-bot
```

Локальный проект на Mac:

```text
/Users/aleksandr_maraenko/Desktop/telegram-max-bot
```

Проект на VPS:

```text
/opt/bots/telegram-max-bot
```

Деплой идет через GitHub Actions.

Схема:

```text
Mac / VSCode
→ git commit
→ git push
→ GitHub Actions
→ SSH на VPS
→ deploy.sh
→ git pull
→ docker compose up -d --build
```

`deploy.sh` выполняет:

```text
git pull
docker compose up -d --build
docker compose ps
docker compose logs --tail=50
```

GitHub Actions использует `appleboy/ssh-action`.

Секреты хранятся в GitHub Secrets, не в коде.

В workflow настроен `paths-ignore` для `README.md`, `PROJECT_CONTEXT.md` и `.env.example`, чтобы изменения только в документации не запускали деплой.

## Docker

Бот запускается через Docker Compose.

Важные настройки:

```yaml
restart: unless-stopped
mem_limit: 256m
cpus: "0.50"
```

Нужно закрепить SQLite-базу через volume:

```yaml
volumes:
  - ./data:/app/data
```

Это важно, чтобы `data/bot.db` не пропадала после пересборки контейнера.

## Переменные окружения

Реальные секреты хранятся только в `.env`.

`.env` не должен попадать в Git.

`.env.example` должен содержать только шаблонные значения.

Текущие/будущие переменные:

```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
APP_ENV=development
SOURCE_RSS_URL=https://habr.com/ru/rss/articles/
DATABASE_PATH=data/bot.db
TELEGRAM_TARGET_CHAT_ID=
CHECK_INTERVAL_SECONDS=300
MAX_BOT_TOKEN=
MAX_TARGET_CHAT_ID=
```

Не хранить в Git:

```text
.env
реальные Telegram/MAX токены
SSH-ключи
IP/секреты деплоя
пароли
cookies
```

## MAX

MAX пока не реализован.

Стратегия:

1. Сначала довести Telegram-версию.
2. Параллельно решать административные вопросы регистрации MAX.
3. После получения токена добавить MAX-адаптер.
4. Не менять `core`, если можно обойтись адаптером.

MAX должен использовать те же внутренние модели и бизнес-логику, что и Telegram.

## Рекомендуемый рабочий процесс

Разработка ведется локально на Mac.

VPS используется в основном для диагностики и проверки деплоя.

Обычный цикл:

```text
1. Сформулировать маленькую задачу.
2. Изменить код.
3. Проверить локально.
4. Проверить git status.
5. Сделать commit.
6. Сделать push.
7. Дождаться GitHub Actions.
8. Проверить работу бота.
```

Команды локально:

```bash
cd /Users/aleksandr_maraenko/Desktop/telegram-max-bot
git status
PYTHONPATH=src python -m telegram_max_bot.main
docker compose up -d --build
docker compose logs -f
git add .
git commit -m "message"
git push
```

Для локальной разработки желательно использовать отдельного dev-бота Telegram, чтобы не конфликтовать с production-ботом на VPS при long polling.

## Task Tracker

- `T01` `Done` (2026-05-28): Минимальный Telegram-бот с командами `/start`, `/help`, `/about` и ответом на текст.
- `T02` `Done` (2026-05-28): Добавлены RSS-импорт, SQLite и команда `/articles`.
- `T03` `Done` (2026-05-28): Добавлена команда `/check` (ручной запуск импорта со статистикой).
- `T04` `Done` (2026-05-28): Реализована синхронизация RSS `created / updated / unchanged` с `content_hash`.
- `T05` `Done` (2026-05-28): Включено сохранение БД через volume `./data:/app/data`.
- `T06` `Done` (2026-05-28): Настроен автодеплой GitHub Actions → VPS → Docker.
- `T07` `Done` (2026-05-28): Проверена работа `/check` и фонового импорта на VPS.
- `T08` `Done` (2026-05-28): Выбран основной режим автоимпорта: фоновый (`CHECK_INTERVAL_SECONDS=300`).
- `T09` `Todo`: Добавить автопубликацию новых статей в Telegram-канал.
- `T10` `Todo`: Проверить RSS заказчика на наличие полного текста и уточнить стратегию загрузки контента.
- `T11` `Todo`: Добавить пользовательские сценарии общения (вопросы, отклики, темы, истории).
- `T12` `Todo`: Подготовить и подключить рабочий MAX-адаптер.
- `T13` `Done` (2026-05-28): Выполнен первичный продуктово-психологический анализ корпуса Дзена, тем автора, аудитории и метрик интереса.
- `T14` `Done` (2026-05-28): Собран сырой архив Дзена `С матушкой за чаем`: 440 статей, 0 ошибок, примерно 1 млн символов текста.
- `T15` `Done` (2026-05-28): Нормализован сырой архив Дзена и импортирован локально в `data/dzen_bot.db`.
- `T16` `Done` (2026-05-28): Нормализованы метрики Дзена: просмотры, время чтения и примерное число комментариев; дата пока хранится как исходный label Дзена.
- `T17` `Todo`: Добавить тематическую разметку статей: РС, брак, дети, вера, психология, женская роль, Москва, культура, быт.
- `T18` `In progress`: Добавить в бота навигацию по рубрикам, самым читаемым и самым обсуждаемым статьям. Уже добавлены `/top` и `/random`; рубрики еще не готовы.
- `T19` `Todo`: Спроектировать сценарии сбора вопросов, откликов, историй и предложений тем от читателей.
- `T20` `Todo`: Добавить модерационный слой для входящих читательских историй и вопросов перед передачей автору.
- `T21` `Todo`: Подготовить отдельный аналитический отчет по корпусу для обсуждения развития канала и контент-стратегии.
- `T22` `Done` (2026-05-28): Скачаны и связаны изображения Дзена: 976 файлов, manifest, таблица `article_images`, 426 статей с cover.
- `T23` `Done` (2026-05-28): Локальная база Дзена и media-папка перенесены на VPS; старая тестовая база Хабра сохранена в backup.

## Ближайшие задачи

1. Выполнить `T17`: тематическая разметка статей.
2. Завершить `T18`: добавить рубрики и навигацию по ним в Telegram-боте.
3. Затем вернуться к `T09`: автопубликация новых статей в Telegram-канал.
4. Позже решить, как обновлять корпус Дзена: повторный ручной импорт, отдельный scraper или другой источник.

## Принятые решения

- 28 мая 2026: проверена работа `/check` и фонового автоимпорта на VPS.
- 28 мая 2026: как основной режим выбран фоновый импорт в боте (`CHECK_INTERVAL_SECONDS`), без cron на текущем этапе.
- 28 мая 2026: проект переосмыслен как коммуникационная система вокруг автора, а не как простой репостер Дзена.
- 28 мая 2026: после анализа корпуса основной смысловой фокус сформулирован как честный разговор о вере, семье, болезни, женской роли, личных границах и жизни без идеальной картинки.
- 28 мая 2026: для будущей навигации в боте выбраны ключевые рубрики: РС, брак и семья, дети, вера без насилия, женская роль и границы, психология, Москва и культура.
- 28 мая 2026: изображения Дзена решено хранить файлами в `data/media/`, а в SQLite хранить только пути, роли, порядок и метаданные.
- 28 мая 2026: текущая база Хабра на VPS считается тестовой; перед заменой на базу Дзена обязательно делать backup.
- 28 мая 2026: после переноса Дзена на VPS фоновый RSS-импорт отключен, чтобы тестовая лента Хабра больше не попадала в production-базу.

## Что не делать

- Не коммитить `.env`.
- Не хранить реальные токены в `.env.example`.
- Не смешивать бизнес-логику с Telegram-обработчиками.
- Не превращать проект в Telegram-only.
- Не добавлять MAX до готовности Telegram/RSS-ядра.
- Не делать большие изменения без маленьких проверяемых шагов.
- Не править вручную код на VPS, кроме экстренной диагностики.
- Не считать текущий RSS-импорт финальной синхронизацией.
